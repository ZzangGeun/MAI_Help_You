"""
캐릭터 정보 API URL 설정 (향후 확장용)
"""
from django.urls import path

urlpatterns = [
    # 향후 캐릭터 API 엔드포인트들이 여기에 추가될 예정
    # 예: path('info/<str:character_name>/', CharacterInfoAPIView.as_view(), name='character-info'),
]
